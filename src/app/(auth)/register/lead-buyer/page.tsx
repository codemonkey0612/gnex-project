import { Suspense } from "react";
import { LeadBuyerRegisterForm } from "./lead-buyer-register-form";

export default function LeadBuyerRegisterPage() {
  return (
    <Suspense>
      <LeadBuyerRegisterForm />
    </Suspense>
  );
}
